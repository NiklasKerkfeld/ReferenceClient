I have provided the text from a reference list below, enclosed in 'BEGIN FILE CONTENT' tags. Your task is to extract and annotate all references found in that specific text.

Tasks:

	Extract all references from the PDF.
	Annotate each reference using only the following XML tags: <author>, <surname>, <given-names>, <year>, <title>, <source>, <editor>, <volume>, <issue>, <fpage>, <lpage>, <publisher>, <other>.
	Generate an XML tag only if the corresponding information is explicitly present in the reference.
	If a specific bibliographic element (e.g., editor, volume, issue, or pages) is not provided, do not generate the respective tag.
	If a reference cannot be clearly identified, mark it as "unidentified" or leave the row empty.

Output format:

	Produce the result as an XML structured file.
	Each reference must appear in a separate <row>.

Formatting rules:

	<author>: use <surname> and <given-names> as sub-tags, with a comma between them.
	<given-names>: always use the full given name as it appears in the reference. Do not abbreviate. (e.g., "Constanze" not "C.")
	<editor>: plain string only, no sub-tags. „In:" and „(Hrsg.)" stay outside the tag as plain text. If multiple editors are present, each editor must be annotated in a separate <editor> tag.
	<source>: use for journal names or book titles in which an article/chapter appears.
	<publisher>: use for the publishing house/institution (e.g. "Springer").
	<other>: use for place of publication (city/location), e.g. <other>Berlin</other>. Place it before <publisher> when both are present, separated by a colon: <other>Berlin</other>: <publisher>Springer</publisher>.
	Page numbers: always use <fpage> and <lpage> separately.

Example: Input: Smidt, W. 2006: Inmitten blutiger Konflikte, Lokale Netzwerke schaffen Stabilität, yousee. Das Magazin der Universität Hamburg 1: 24-25.
Expected output: <row><author><surname>Smidt</surname>, <given-names>W.</given-names></author> <year>2006</year>: <title>Inmitten blutiger Konflikte, Lokale Netzwerke schaffen Stabilität</title>, <source>yousee. Das Magazin der Universität Hamburg</source> <volume>1</volume>: <fpage>24</fpage>-<lpage>25</lpage></row>